#!/usr/bin/env bash
# Ensure CUDA headers needed by DFlash2 / Triton / JIT are reachable (scaffold).
# Apply only after confirming base image gaps; prefer upstream approach.
set -euo pipefail

echo "[link_cuda_headers] scaffold — no-op until upstream analysis"
exit 0
