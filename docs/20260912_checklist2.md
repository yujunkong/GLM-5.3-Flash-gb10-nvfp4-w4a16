아래 순서대로 **그대로 Cursor에 전달해서 실행**하면 돼. 실제 경로/컨테이너명은 먼저 탐색하도록 해서 임의로 가정하지 않게 했다. 최근 확인된 GB10 이슈에서도 sparse-indexer 메모리 churn과 64 MiB logits budget / `expandable_segments`가 실제로 보고되어 있으므로, 이 부분은 별도 단계로 남긴다. 

# 실행 원칙

대상:

```text
Canada-Quant GLM-5.3-Flash W4A16
DFlash2 K=7
2× DGX Spark / GB10 / SM121
TP=2
```

현재 production 설정은 변경하지 않는다.

이미 수행한 다음 항목은 **재실험 금지**:

```text
2400 MHz
async scheduling
busy_loop
worker memory cap
기존 top-k fallback
DFlash2 K=7
TOP_K=32
walk=edge
ACC_PROBE=0
SM121 MLA
FlashInfer SM121 build
Marlin _C SM121 build
KDA in_proj
shared_experts
lm_head
CUDA workspace
NCCL
CUDA Graph
Expert Parallel
BF16 KV
block/chunk 변경
```

각 단계는 **독립 branch/commit**으로 테스트한다.

---

# 0. 작업 위치와 현재 상태 확인

## spark1

```bash
cd ~/glm-5.3-flash-w4a16-2x-DGX-Sparks 2>/dev/null || {
  echo "REPO NOT FOUND"
  exit 1
}

pwd
git status --short
git branch --show-current
git log -1 --oneline
```

## spark2

동일한 repo가 있으면:

```bash
cd ~/glm-5.3-flash-w4a16-2x-DGX-Sparks
pwd
git status --short
git branch --show-current
git log -1 --oneline
```

없으면 **파일을 임의로 만들지 말고 중단**.

---

# 1. 현재 Docker / 이미지 확인

두 노드에서:

```bash
docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Status}}'
```

현재 실행 중인 vLLM container 이름을 변수로 잡는다.

```bash
export VLLM_CTN=$(docker ps --format '{{.Names}}\t{{.Image}}' \
  | grep -E 'glm|vllm' \
  | head -1 \
  | cut -f1)

echo "$VLLM_CTN"
```

이미지 확인:

```bash
docker inspect "$VLLM_CTN" \
  --format 'IMAGE={{.Config.Image}}'
```

컨테이너 내부 버전:

```bash
docker exec "$VLLM_CTN" python - <<'PY'
import vllm, torch
print("vLLM:", vllm.__version__)
print("Torch:", torch.__version__)
print("CUDA:", torch.version.cuda)
PY
```

### 중단

현재 이미지가 예상한 production 이미지가 아니면 이후 작업 중단.

```text
radixark/vllm-glm53-flash:sm121-v11-dflash2
```

또는 현재 실제 production digest가 확인되어야 한다.

---

# 2. Production baseline 저장

현재 실행 command:

```bash
docker inspect "$VLLM_CTN" \
  --format '{{json .Config.Cmd}}' > /tmp/glm53-cmd.json

docker inspect "$VLLM_CTN" \
  --format '{{json .Config.Env}}' > /tmp/glm53-env.json
```

GPU 상태:

```bash
nvidia-smi --query-gpu=index,name,temperature.gpu,clocks.sm,utilization.gpu,memory.used,memory.total \
  --format=csv
```

환경:

```bash
env | sort | grep -E \
'^(MODEL|DRAFT|DFLASH|VLLM|CUDA|NCCL|PYTORCH|MOE|GLM|HEAD_|WORKER_)' \
> /tmp/glm53-host-env.txt
```

Git 기준점:

```bash
git tag -f glm53-w4a16-baseline-20260912
git rev-parse HEAD
```

### 기준값

```text
TPS: 34.7–35.5 tok/s
acceptance: 약 0.439
```

### 중단

baseline이 기존 범위보다:

```text
TPS -5% 이하
acceptance -0.03p 이하
```

면 패치 작업 중단.

---

# 3. 최신 upstream에서 W4A16 관련 변경 검색

먼저 현재 repo에 upstream remote 확인:

```bash
git remote -v
```

없다면 원격 URL을 임의 생성하지 말고 현재 repo의 upstream 설정을 확인한다.

이미 upstream이 있으면:

```bash
git fetch --all --prune
```

vLLM 소스 위치 확인:

```bash
docker exec "$VLLM_CTN" sh -lc \
'python - <<PY
import vllm, os
print(os.path.dirname(vllm.__file__))
PY'
```

소스가 `/workspace` 등에 있다면:

```bash
docker exec "$VLLM_CTN" sh -lc \
'find / -path "*/vllm/models/glm5next*" -o -path "*/vllm/model_executor*" 2>/dev/null | head -50'
```

검색:

```bash
docker exec "$VLLM_CTN" sh -lc \
'grep -RniE "GLM5Next|glm5next|sparse.*index|KeyPool|persistent_topk|DFlash|spec_decode" \
/workspace /vllm /opt 2>/dev/null | head -200'
```

현재 repo에서도:

```bash
rg -n \
'GLM5Next|glm5next|sparse.?index|KeyPool|persistent_topk|DFlash|spec_decode' \
.
```

### 결과 저장

```bash
rg -n \
'GLM5Next|glm5next|sparse.?index|KeyPool|persistent_topk|DFlash|spec_decode' \
. > /tmp/glm53-current-code-search.txt
```

### 판정

실제 W4A16 + TP2 + DFlash2 decode 경로에 해당하지 않는 변경은 전부 제외.

**여기서는 아직 패치하지 않는다.**

---

# 4. sparse-indexer allocation 점검

실제 관련 파일부터 찾는다.

```bash
rg -n \
'logits|index_topk|persistent_topk|KeyPoolIndexer|sparse_indexer|kpool' \
vllm/ 2>/dev/null \
> /tmp/sparse-indexer-search.txt
```

후보 파일:

```bash
grep -E '\.py:' /tmp/sparse-indexer-search.txt \
| cut -d: -f1 \
| sort -u
```

allocation 패턴:

```bash
rg -n \
'torch\.(empty|zeros|ones|full|randn)|empty_like|zeros_like|torch\.cat|torch\.stack|cudaMalloc|allocate|workspace' \
$(cat /tmp/sparse-indexer-search.txt | cut -d: -f1 | sort -u) \
2>/dev/null
```

### 확인해야 할 것

반복 decode마다 동일 shape의:

```text
torch.empty
torch.zeros
workspace
logits buffer
top-k buffer
```

가 새로 할당되는지 확인.

---

# 5. allocation 재사용 패치

다음 조건일 때만 patch:

```text
동일 shape 반복
+
request/engine lifetime 동안 재사용 가능
+
계산 결과 동일
+
allocator 호출 감소
```

수정 전 branch:

```bash
git switch -c opt/sparse-indexer-buffer-reuse
```

수정 후:

```bash
git diff --check
git diff
```

Python compile:

```bash
python -m compileall -q vllm
```

commit:

```bash
git add .
git commit -m "opt: reuse sparse indexer temporary buffers"
```

### A/B

baseline:

```bash
# 기존 production 실행
```

patch:

```bash
# 동일 command
```

동일 workload로 측정:

```text
TPS
acceptance
GPU memory
CUDA allocated
CUDA reserved
```

### 중단

즉시 revert:

```text
TPS >2% 감소
acceptance 감소
memory 증가
CUDA error
output mismatch
```

개선폭이 **+2% 미만이면 patch 폐기**.

---

# 6. sparse-indexer logits budget 확인

현재 소스에 해당 설정이 있는지:

```bash
rg -n \
'VLLM_SPARSE_INDEXER_MAX_LOGITS_MB|MAX_LOGITS_MB|max_logits' \
vllm/ .
```

환경변수 지원이 이미 있으면:

```bash
export VLLM_SPARSE_INDEXER_MAX_LOGITS_MB=64
```

컨테이너 실행 환경에 넣어 테스트한다.

지원 코드가 없다면 최신 upstream에서 실제 구현 위치를 검색:

```bash
git log --all --oneline --grep='logits budget' -i
git log --all --oneline --grep='sparse indexer' -i
git log --all --oneline --grep='unified memory' -i
```

원격 최신 commit 검색 결과가 있으면 해당 commit의 파일만 확인한다.

### 64 MiB A/B

```text
baseline
vs
VLLM_SPARSE_INDEXER_MAX_LOGITS_MB=64
```

긴 prompt에서 특히 확인:

```text
128K
200K
230K+
```

### 중단

다음이면 즉시 제거:

```text
TPS -2% 이상
acceptance 감소
output correctness 문제
request failure
OOM 증가
```

TPS가 동일하고 long-context memory 안정성만 좋아지면 **안정성 패치로 유지 가능**.

최근 vLLM #55569에서 실제로 64 MiB budget이 ~230K prefill unified-memory 문제를 해결한 사례가 있으므로 이 단계는 별도 검증 가치가 있다.

---

# 7. `expandable_segments` A/B

코드 수정하지 않는다.

현재 환경:

```bash
docker exec "$VLLM_CTN" sh -lc \
'env | grep PYTORCH_CUDA_ALLOC_CONF || true'
```

A:

```text
PYTORCH_CUDA_ALLOC_CONF unset
```

B:

```bash
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
```

실제 container 환경에 적용한 뒤 동일 command로 재시작.

30분 이상 측정:

```text
TPS
GPU memory
host memory
allocator reserved
allocator inactive
```

### 중단

```text
TPS >2% 감소
latency 증가
acceptance 감소
memory 증가
```

면 즉시 원복.

최근 GB10/GLM5.3 이슈에서도 `expandable_segments:True`가 동일 unified-memory exhaustion 회피책으로 보고됐다.

---

# 8. 현재 Top-K fallback과 최신 코드 비교

현재 이미지:

```bash
docker exec "$VLLM_CTN" sh -lc \
'find / -iname "*topk*" -o -iname "*sparse*index*" 2>/dev/null \
| grep -E "vllm|patch|glm" | head -100'
```

repo:

```bash
rg -n \
'persistent_topk|topk.*fallback|fallback.*topk|index_topk' \
.
```

mmastrac 최신 소스가 필요하면 별도 temporary clone:

```bash
cd /tmp
git clone --depth 1 https://github.com/mmastrac/glm-5.3-flash-4x-gx10.git mmastrac-glm53
```

검색:

```bash
rg -n \
'persistent_topk|topk|fallback|sparse' \
mmastrac-glm53/image mmastrac-glm53/scripts
```

현재 코드와 diff:

```bash
diff -ru \
/path/to/current/patches \
/tmp/mmastrac-glm53/image/patches \
| less
```

**실제 경로는 앞의 `find` 결과로 결정한다.**

mmastrac 최신 레시피에는 GB10 `persistent_topk`를 fallback으로 보내는 별도 패치가 존재한다.

### 판정

동일 기능:

```text
PATCH = NONE
```

차이가 있을 때만 해당 부분 검토.

---

# 9. DFlash2 scheduler synchronization 검색

현재 vLLM source에서:

```bash
rg -n \
'cuda.*synchron|synchronize\(|\.item\(\)|\.cpu\(\)|\.tolist\(\)' \
vllm/ \
> /tmp/vllm-sync-search.txt
```

DFlash 관련:

```bash
rg -n \
'DFlash|dflash|speculative|accepted_tokens|num_accepted' \
vllm/ \
> /tmp/dflash-search.txt
```

두 결과 교차 확인:

```bash
grep -E \
'cuda|synchron|\.item|\.cpu|tolist' \
/tmp/dflash-search.txt
```

실제 hot path에 있는지 call chain을 따라간다.

### 패치 조건

반드시:

```text
DFlash2 decode hot path
+
불필요한 CPU/GPU synchronization
+
동일 결과 유지 가능
```

이어야 한다.

---

# 10. synchronization 패치 A/B

branch:

```bash
git switch -c opt/dflash-sync
```

수정:

```bash
git diff --check
python -m compileall -q vllm
```

commit:

```bash
git add .
git commit -m "perf: remove unnecessary decode synchronization"
```

측정:

```text
TPS
iteration latency
CPU utilization
acceptance
```

### 중단

```text
TPS 개선 <2% → 폐기
acceptance 감소 → 즉시 revert
latency 증가 → 즉시 revert
CUDA/stream race → 즉시 revert
```

---

# 11. Marlin integration만 검색

Marlin kernel은 건드리지 않는다.

검색:

```bash
rg -n \
'marlin|MoE|expert.*dispatch|workspace|contiguous|copy_|to\(.*dtype|astype' \
vllm/ \
> /tmp/marlin-integration.txt
```

확인 대상:

```text
expert dispatch
activation layout
weight layout
dtype conversion
workspace allocation
tensor copy
```

다음 패턴만 후보:

```text
copy
+
동일 데이터
+
불필요한 layout conversion
+
반복 allocation
```

### 패치

```bash
git switch -c opt/marlin-integration
```

수정 후:

```bash
git diff --check
python -m compileall -q vllm
```

### 중단

```text
TPS < baseline
acceptance 감소
expert output mismatch
memory 증가
```

이면 즉시 revert.

**+2% 미만이면 폐기.**

---

# 12. BF16 integration 검색

현재 hot path인 BF16 linear 주변만 확인:

```bash
rg -n \
'q_b_proj|o_proj|gate_up_proj|lm_head|in_proj_qkvbfg_a' \
vllm/
```

copy/conversion 검색:

```bash
rg -n \
'contiguous\(|clone\(|copy_|to\(.*bfloat16|to\(.*dtype|view\(|reshape\(' \
vllm/ \
> /tmp/bf16-integration.txt
```

call chain을 확인해서 실제 decode 중 호출되는 것만 남긴다.

### 패치 조건

```text
동일 BF16 계산
+
copy/layout conversion/workspace 제거
```

일 때만.

cuBLASLt GEMM kernel 자체는 교체하지 않는다.

### 중단

```text
TPS 개선 <2%
acceptance 감소
numerical mismatch
memory 증가
```

면 폐기.

---

# 13. 장시간 TPS degradation instrumentation

이 단계는 **패치보다 먼저 원인을 잡기 위해 별도로 실행**한다.

spark1:

```bash
mkdir -p ~/glm53-logs

while true; do
  date '+%F %T' >> ~/glm53-logs/gpu.csv

  nvidia-smi \
    --query-gpu=index,temperature.gpu,clocks.sm,utilization.gpu,memory.used,memory.total \
    --format=csv,noheader \
    >> ~/glm53-logs/gpu.csv

  free -h >> ~/glm53-logs/memory.log

  sleep 10
done
```

CPU:

```bash
mpstat -P ALL 10 > ~/glm53-logs/cpu.log
```

메모리:

```bash
vmstat 10 > ~/glm53-logs/vmstat.log
```

프로세스:

```bash
pidstat -rud -p "$(pgrep -f 'vllm' | head -1)" 10 \
  > ~/glm53-logs/vllm-pidstat.log
```

컨테이너:

```bash
docker stats --no-stream "$VLLM_CTN" \
  >> ~/glm53-logs/docker-stats.log
```

반복:

```bash
while true; do
  date '+%F %T'
  docker stats --no-stream "$VLLM_CTN"
  sleep 30
done > ~/glm53-logs/docker-stats.log
```

### 60~120분 실행

동일한 OpenCode workload를 유지한다.

기록:

```text
TPS
acceptance
GPU clock
GPU utilization
GPU temperature
CPU utilization
host memory
GPU/unified memory
KV usage
request count
```

### 판정

```text
GPU clock ↓
→ clock/thermal

memory ↑
→ allocator/unified memory

CPU ↑
→ scheduler/runtime

GPU util 유지 + TPS ↓
→ GPU kernel/runtime

acceptance ↓
→ DFlash2
```

원인이 명확하지 않으면 **추가 패치 금지**.

---

# 14. 최종 통합 순서

검증을 통과한 패치만 다음 순서로 통합한다.

```text
1. sparse-indexer buffer reuse
2. logits budget / allocator 안정화
3. decode synchronization
4. Marlin integration
5. BF16 integration
```

각 단계마다 commit:

```bash
git log --oneline --decorate -10
```

최종 branch:

```bash
git switch <production-branch>
git merge --no-ff <validated-branch>
```

최종 검증:

```bash
git diff glm53-w4a16-baseline-20260912..HEAD --stat
git diff glm53-w4a16-baseline-20260912..HEAD
```

---

# 최종 중단 기준

다음 중 하나면 **해당 항목에서 즉시 중단**:

```text
TPS >2% 하락
acceptance 하락
품질/출력 mismatch
CUDA error
request failure
memory 증가
latency 증가
```

다음이면 **그 후보 전체를 폐기**:

```text
성능 개선 <2%
```

다음이면 **전체 신규 최적화 작업 종료**:

```text
모든 신규 후보가 +2% 미만
또는
W4A16 품질을 유지하면서 추가 개선 가능한 코드가 없음
```

최종 production 통과 기준:

```text
TPS        >= 34.7 tok/s
acceptance >= 0.439
품질       = baseline
장시간 TPS degradation <= baseline
```

**40 tok/s는 목표값이지 강제 패치 기준이 아니다. 안전하게 얻을 수 있는 개선이 없으면 40 tok/s를 위해 품질/안정성을 희생하지 않는다.**